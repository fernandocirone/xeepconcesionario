@model Localidad

@{
    ViewData["Title"] = "Editar Localidad";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-lg-6">

            <div class="card shadow-sm border-0">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0">✏️ Editar Localidad</h4>
                </div>
                <div class="card-body">
                    <form asp-action="Edit">
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>
                        <input type="hidden" asp-for="LocalidadId" />

                        <div class="mb-3">
                            <label asp-for="ProvinciaId" class="form-label"></label>
                            <select asp-for="ProvinciaId" class="form-select" asp-items="ViewBag.ProvinciaId"></select>
                            <span asp-validation-for="ProvinciaId" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="RegionId" class="form-label"></label>
                            <select asp-for="RegionId" class="form-select" asp-items="ViewBag.RegionId"></select>
                            <span asp-validation-for="RegionId" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="NombreLocalidad" class="form-label"></label>
                            <input asp-for="NombreLocalidad" class="form-control" />
                            <span asp-validation-for="NombreLocalidad" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="CodigoPostal" class="form-label"></label>
                            <input asp-for="CodigoPostal" class="form-control" />
                            <span asp-validation-for="CodigoPostal" class="text-danger"></span>
                        </div>

                        <div class="d-flex justify-content-between">
                            <a asp-action="Index" class="btn btn-secondary">⬅️ Volver</a>
                            <button type="submit" class="btn btn-primary">💾 Guardar cambios</button>
                        </div>
                    </form>
                </div>
            </div>

        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
